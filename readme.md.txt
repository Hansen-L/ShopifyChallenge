# Shopify Challenge

An image repository with upload, delete and login functionality. This is a beta version, to be integrated with other flask projects in the future.

## Installation

Use the package manager [pip](https://pip.pypa.io/en/stable/) to install requirements.

```bash
pip install --file requirements.txt
```

## Usage

Open terminal in project directory. 

```python
python run.py
```
